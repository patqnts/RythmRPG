using UnityEngine;

namespace RythmRPG.Combat
{
    /// <summary>Keeps this object and its children visible when <see cref="SpaceCombatBackdrop"/> hides the scene for combat.</summary>
    [DisallowMultipleComponent]
    public sealed class SpaceBackdropKeepVisible : MonoBehaviour
    {
    }
}
