using UnityEngine;

[CreateAssetMenu(menuName = "NoteObjectCollection")]
public class NoteObjectsManager : ScriptableObject
{
    // Start is called before the first frame update
    public GameObject clusterNote;
}
